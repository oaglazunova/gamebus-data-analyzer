from __future__ import annotations

import os
import warnings
import argparse
import hashlib

import pandas as pd

from typing import Any, Dict, Callable
from datetime import datetime, timezone

from src.trajectory.common import (
    TrajectoryAuditConfig,
    write_json,
)
from src.trajectory.data_profile import (
    run_data_profile,
)
from src.trajectory.event_normalization import (
    run_event_normalization,
)
from src.trajectory.participation_episode_builder import (
    run_participation_episode_builder,
)
from src.trajectory.observation_window import (
    run_observation_window,
)
from src.trajectory.participant_state_daily import (
    run_participant_state_daily,
)
from src.trajectory.inactivity_gap_analysis import (
    run_inactivity_gap_analysis,
)
from src.trajectory.inactivity_threshold_analysis import (
    run_inactivity_threshold_analysis,
)
from src.trajectory.domain_tool_engagement import (
    run_domain_tool_engagement,
)
from src.trajectory.data_quality_state import (
    run_data_quality_state,
)
from src.trajectory.candidate_patterns import (
    run_candidate_patterns,
)
from src.trajectory.case_export import (
    run_case_export,
)
from src.trajectory.engagement import (
    explicit_engagement_mask,
)



TrajectoryProgressCallback = Callable[
    [
        str,
        str,
    ],
    None,
]


def _notify_progress(
    progress_callback: (
        TrajectoryProgressCallback
        | None
    ),
    stage: str,
    message: str,
) -> None:
    if progress_callback is not None:
        progress_callback(
            stage,
            message,
        )


def _validate_inputs(
    config: TrajectoryAuditConfig,
) -> None:
    """
    Validate only the files required for the
    campaign-export trajectory audit.

    users.xlsx and credentials are deliberately
    NOT required.
    """

    required_files = {
        "campaign data": (
            config.campaign_data_path
        ),

        "campaign description": (
            config.campaign_desc_path
        ),
    }

    missing = [
        (
            label,
            path,
        )
        for (
            label,
            path,
        )
        in required_files.items()
        if not os.path.isfile(
            path
        )
    ]

    if not missing:
        return

    details = "\n".join(
        f"  {label}: {path}"
        for label, path
        in missing
    )

    raise FileNotFoundError(
        "Trajectory Audit requires the "
        "campaign export files:\n"
        f"{details}"
    )



def _sha256(
    path: str,
) -> str:
    """
    Compute a SHA-256 checksum for an input file.

    This lets us later verify exactly which campaign
    export produced a trajectory audit.
    """

    digest = hashlib.sha256()

    with open(
        path,
        "rb",
    ) as file:

        for chunk in iter(
            lambda: file.read(
                1024 * 1024
            ),
            b"",
        ):
            digest.update(
                chunk
            )

    return digest.hexdigest()


def _generated_files(
    output_dir: str,
) -> list[str]:
    """
    Return paths generated by the current audit,
    relative to the audit output directory.
    """

    result = []

    if not os.path.isdir(
        output_dir
    ):
        return result

    for root, _, files in os.walk(
        output_dir
    ):

        for filename in files:

            path = os.path.join(
                root,
                filename,
            )

            relative = os.path.relpath(
                path,
                output_dir,
            )

            # The manifest is written after this scan.
            if (
                relative
                == "audit_manifest.json"
            ):
                continue

            result.append(
                relative
            )

    return sorted(
        result
    )


def _write_audit_manifest(
    config: TrajectoryAuditConfig,
) -> str:
    """
    Write reproducibility metadata for the completed
    trajectory audit.
    """

    manifest = {
        "completed_at_utc": (
            datetime.now(
                timezone.utc
            ).isoformat()
        ),

        "inputs": {
            "campaign_data": {
                "path": os.path.abspath(
                    config.campaign_data_path
                ),

                "sha256": _sha256(
                    config.campaign_data_path
                ),
            },

            "campaign_description": {
                "path": os.path.abspath(
                    config.campaign_desc_path
                ),

                "sha256": _sha256(
                    config.campaign_desc_path
                ),
            },
        },

        "configuration": {
            "episode_gap_days": (
                config.episode_gap_days
            ),

            "observation_start_grace_days": (
                config
                .observation_start_grace_days
            ),

            "analysis_cutoff": (
                config.analysis_cutoff
            ),

            "pattern_recent_window_days": (
                config
                .pattern_recent_window_days
            ),

            "pattern_reference_window_days": (
                config
                .pattern_reference_window_days
            ),

            "pattern_min_reference_events": (
                config
                .pattern_min_reference_events
            ),

            "pattern_decline_ratio": (
                config.pattern_decline_ratio
            ),

            "pattern_repeated_gap_count": (
                config
                .pattern_repeated_gap_count
            ),

            "case_export_max_cases": (
                config
                .case_export_max_cases
            ),

            "analysis_participant_ids": (
                sorted(
                    config.analysis_participant_ids
                )
                if (
                        config.analysis_participant_ids
                        is not None
                )
                else None
            ),
        },

        "output_directory": (
            os.path.abspath(
                config.output_dir
            )
        ),

        "generated_files": (
            _generated_files(
                config.output_dir
            )
        ),

        "notes": {
            "candidate_patterns": (
                "Exploratory descriptive signals; "
                "not validated dropout predictions "
                "or causal effects."
            ),

            "missing_streams": (
                "Unavailable streams are represented "
                "as unavailable rather than zero "
                "participant behavior."
            ),
        },
    }

    manifest_path = os.path.join(
        config.output_dir,
        "audit_manifest.json",
    )

    write_json(
        manifest_path,
        manifest,
    )

    return manifest_path



def _build_audit_summary(
    results: Dict[str, Any],
) -> Dict[str, Any]:
    """
    Build a compact cross-campaign summary from
    the completed trajectory-audit outputs.

    This file is intended for robustness testing
    and comparison across historical campaigns.
    """

    events = results[
        "normalized_events"
    ]

    episodes_result = results[
        "participation_episodes"
    ]

    # participation_episode_builder currently returns
    # (explicit_engagement_events, episodes).
    if isinstance(
            episodes_result,
            tuple,
    ):
        _, episodes = (
            episodes_result
        )
    else:
        episodes = (
            episodes_result
        )

    explicit_event_count = int(
        explicit_engagement_mask(
            events
        ).sum()
    )

    state = results[
        "participant_state_daily"
    ]

    gaps = results[
        "inactivity_gaps"
    ]

    transitions = results[
        "inactivity_threshold_transitions"
    ]

    domain_tool = results[
        "domain_tool_engagement"
    ]

    quality_result = results[
        "data_quality"
    ]

    if isinstance(
        quality_result,
        tuple,
    ):
        quality, quality_summary = (
            quality_result
        )
    else:
        quality = quality_result
        quality_summary = {}

    patterns = results[
        "candidate_patterns"
    ]

    cases = results[
        "case_export"
    ]

    observation_window_result = (
        results.get(
            "observation_window",
            {},
        )
    )

    cohort_result = (
        observation_window_result.get(
            "cohort",
            {},
        )
        if isinstance(
            observation_window_result,
            dict,
        )
        else {}
    )

    cohort_source = (
        cohort_result.get(
            "source",
            "unavailable",
        )
    )

    # -------------------------------------------------
    # Cohort
    # -------------------------------------------------

    cohort_size = (
        int(
            quality[
                "participant_id"
            ].nunique()
        )
        if not quality.empty
        else 0
    )

    explicit_participants = (
        int(
            quality.loc[
                quality[
                    "has_explicit_engagement"
                ],
                "participant_id",
            ].nunique()
        )
        if (
            not quality.empty
            and "has_explicit_engagement"
            in quality.columns
        )
        else 0
    )

    # -------------------------------------------------
    # Observed cohort evidence
    # -------------------------------------------------

    observed_participants = (
        int(
            events[
                "participant_id"
            ]
            .dropna()
            .nunique()
        )
        if (
            not events.empty
            and "participant_id"
            in events.columns
        )
        else 0
    )

    participants_without_events = (
        max(
            cohort_size
            - observed_participants,
            0,
        )
    )

    observed_participant_coverage = (
        observed_participants
        / cohort_size
        if cohort_size > 0
        else None
    )

    # -------------------------------------------------
    # State at cutoff
    # -------------------------------------------------

    cutoff_states = {}

    if not state.empty:

        state_dates = pd.to_datetime(
            state[
                "date"
            ],
            errors="coerce",
        )

        cutoff = (
            state_dates.max()
        )

        cutoff_rows = state.loc[
            state_dates
            == cutoff
        ]

        cutoff_states = {
            str(name): int(
                count
            )
            for name, count
            in cutoff_rows[
                "engagement_state"
            ]
            .value_counts(
                dropna=False
            )
            .items()
        }

    # -------------------------------------------------
    # Gap thresholds
    # -------------------------------------------------

    gap_thresholds = {}

    if not gaps.empty:

        for threshold in (
            7,
            14,
            21,
        ):

            column = (
                f"reached_{threshold}d"
            )

            reached = gaps.loc[
                gaps[
                    column
                ]
            ]

            gap_thresholds[
                str(threshold)
            ] = {
                "gaps": int(
                    len(
                        reached
                    )
                ),

                "observed_reengagements": int(
                    reached[
                        "reengaged"
                    ].sum()
                ),

                "right_censored": int(
                    reached[
                        "right_censored"
                    ].sum()
                ),
            }

    # -------------------------------------------------
    # Candidate patterns
    # -------------------------------------------------

    pattern_counts = {}

    if not patterns.empty:

        pattern_counts = {
            str(name): int(
                count
            )
            for name, count
            in patterns[
                "pattern_type"
            ]
            .value_counts()
            .items()
        }

    # -------------------------------------------------
    # Domain mapping
    # -------------------------------------------------

    unique_activity_events = (
        int(
            domain_tool[
                "event_id"
            ].nunique()
        )
        if not domain_tool.empty
        else 0
    )

    unmapped_activity_events = (
        int(
            domain_tool.loc[
                domain_tool[
                    "domain"
                ]
                == "unmapped",
                "event_id",
            ].nunique()
        )
        if not domain_tool.empty
        else 0
    )

    multi_domain_events = (
        int(
            domain_tool.loc[
                domain_tool[
                    "domain_membership_count"
                ]
                > 1,
                "event_id",
            ].nunique()
        )
        if not domain_tool.empty
        else 0
    )

    # -------------------------------------------------
    # Final compact summary
    # -------------------------------------------------

    return {
                "cohort": {
            "participants": (
                cohort_size
            ),

            "participants_with_any_observed_event": (
                observed_participants
            ),

            "participants_without_observed_events": (
                participants_without_events
            ),

            "observed_participant_coverage": (
                observed_participant_coverage
            ),

            "participants_with_explicit_engagement": (
                explicit_participants
            ),

            "participants_without_explicit_engagement": (
                cohort_size
                - explicit_participants
            ),

            "cohort_source": (
                cohort_source
            ),
        },

        "events": {
            "normalized_events": int(
                len(
                    events
                )
            ),

            "explicit_engagement_events": (
                explicit_event_count
            ),
        },

        "episodes": {
            "participation_episodes": int(
                len(
                    episodes
                )
            ),
        },

        "state_at_cutoff": (
            cutoff_states
        ),

        "inactivity_gaps": {
            "total": int(
                len(
                    gaps
                )
            ),

            "closed_with_reengagement": (
                int(
                    gaps[
                        "reengaged"
                    ].sum()
                )
                if not gaps.empty
                else 0
            ),

            "right_censored": (
                int(
                    gaps[
                        "right_censored"
                    ].sum()
                )
                if not gaps.empty
                else 0
            ),

            "thresholds": (
                gap_thresholds
            ),
        },

        "threshold_signals": {
            "total": int(
                len(
                    transitions
                )
            ),
        },

        "domain_mapping": {
            "activity_events": (
                unique_activity_events
            ),

            "unmapped_activity_events": (
                unmapped_activity_events
            ),

            "multi_domain_activity_events": (
                multi_domain_events
            ),
        },

        "candidate_patterns": {
            "rows": int(
                len(
                    patterns
                )
            ),

            "participants": (
                int(
                    patterns[
                        "participant_id"
                    ].nunique()
                )
                if not patterns.empty
                else 0
            ),

            "by_type": (
                pattern_counts
            ),
        },

        "case_export": {
            "cases": int(
                len(
                    cases
                )
            ),
        },

        "data_quality": (
            quality_summary
        ),
    }



def run_trajectory_audit(
    config: TrajectoryAuditConfig | None = None,
    *,
    progress_callback: (
        TrajectoryProgressCallback
        | None
    ) = None,
) -> Dict[str, Any]:
    """
    Run the complete Trajectory Audit.

    This workflow is independent of participant
    credentials and does not authenticate against
    GameBus.

    Primary inputs:

        config/campaign_data.zip
        config/campaign_desc.xlsx

    Optional previously extracted JSON may enrich
    data-quality reporting, but is not required.
    """

    config = (
        config
        or TrajectoryAuditConfig()
    )

    _notify_progress(
        progress_callback,
        "validating",
        "Validating trajectory audit inputs",
    )

    _validate_inputs(
        config
    )

    os.makedirs(
        config.output_dir,
        exist_ok=True,
    )

    # The supplied campaign workbook has no default
    # Excel style. openpyxl warns about this every
    # time it is loaded, but it does not affect data.
    warnings.filterwarnings(
        "ignore",
        message=(
            "Workbook contains no default style, "
            "apply openpyxl's default"
        ),
        category=UserWarning,
        module=(
            "openpyxl.styles.stylesheet"
        ),
    )

    results: Dict[
        str,
        Any,
    ] = {}

    print()
    print(
        "=" * 60
    )
    print(
        "TRAJECTORY AUDIT"
    )
    print(
        "=" * 60
    )



    # -------------------------------------------------
    # 1. Data profile
    # -------------------------------------------------
    _notify_progress(
        progress_callback,
        "data_profile",
        "Profiling campaign data",
    )

    print()
    print(
        "[1/11] Data profile"
    )

    results[
        "data_profile"
    ] = run_data_profile(
        config
    )

    # -------------------------------------------------
    # 2. Canonical events
    # -------------------------------------------------
    _notify_progress(
        progress_callback,
        "event_normalization",
        "Normalizing campaign events",
    )

    print()
    print(
        "[2/11] Event normalization"
    )

    results[
        "normalized_events"
    ] = run_event_normalization(
        config
    )

    # -------------------------------------------------
    # 3. Participation episodes
    # -------------------------------------------------
    _notify_progress(
        progress_callback,
        "participation_episodes",
        "Reconstructing participation episodes",
    )

    print()
    print(
        "[3/11] Participation episodes"
    )

    results[
        "participation_episodes"
    ] = (
        run_participation_episode_builder(
            config
        )
    )

    # -------------------------------------------------
    # 4. Observation window
    # -------------------------------------------------
    _notify_progress(
        progress_callback,
        "observation_window",
        "Determining the observation window",
    )

    print()
    print(
        "[4/11] Observation window"
    )

    results[
        "observation_window"
    ] = run_observation_window(
        config
    )

    # -------------------------------------------------
    # 5. ParticipantState(t)
    # -------------------------------------------------

    print()
    print(
        "[5/11] ParticipantState(t)"
    )

    _notify_progress(
        progress_callback,
        "participant_state",
        "Building daily participant states",
    )

    results[
        "participant_state_daily"
    ] = (
        run_participant_state_daily(
            config
        )
    )

    # -------------------------------------------------
    # 6. Inactivity gaps
    # -------------------------------------------------

    print()
    print(
        "[6/11] Inactivity gaps"
    )

    _notify_progress(
        progress_callback,
        "inactivity_gaps",
        "Analyzing inactivity gaps",
    )

    results[
        "inactivity_gaps"
    ] = run_inactivity_gap_analysis(
        config
    )

    # -------------------------------------------------
    # 7. Threshold transitions
    # -------------------------------------------------

    print()
    print(
        "[7/11] Inactivity threshold transitions"
    )

    _notify_progress(
        progress_callback,
        "threshold_transitions",
        "Detecting inactivity threshold transitions",
    )

    results[
        "inactivity_threshold_transitions"
    ] = (
        run_inactivity_threshold_analysis(
            config
        )
    )

    # -------------------------------------------------
    # 8. Domain/tool engagement
    # -------------------------------------------------

    print()
    print(
        "[8/11] Domain/tool engagement"
    )

    _notify_progress(
        progress_callback,
        "domain_tool_engagement",
        "Analyzing domain and tool engagement",
    )

    results[
        "domain_tool_engagement"
    ] = run_domain_tool_engagement(
        config
    )

    # -------------------------------------------------
    # 9. Data quality
    # -------------------------------------------------

    print()
    print(
        "[9/11] Data quality"
    )

    _notify_progress(
        progress_callback,
        "data_quality",
        "Assessing trajectory data quality",
    )

    results[
        "data_quality"
    ] = run_data_quality_state(
        config
    )

    # -------------------------------------------------
    # 10. Candidate patterns
    # -------------------------------------------------

    print()
    print(
        "[10/11] Candidate patterns"
    )

    _notify_progress(
        progress_callback,
        "candidate_patterns",
        "Detecting candidate trajectory patterns",
    )

    results[
        "candidate_patterns"
    ] = run_candidate_patterns(
        config
    )

    # -------------------------------------------------
    # 11. Cases and plots
    # -------------------------------------------------

    print()
    print(
        "[11/11] Case exports and plots"
    )

    _notify_progress(
        progress_callback,
        "case_export",
        "Generating participant cases and plots",
    )

    results[
        "case_export"
    ] = run_case_export(
        config
    )

    print()
    print(
        "Writing audit summary"
    )

    _notify_progress(
        progress_callback,
        "summary",
        "Writing audit summary",
    )

    audit_summary = (
        _build_audit_summary(
            results
        )
    )

    summary_path = os.path.join(
        config.output_dir,
        "audit_summary.json",
    )

    write_json(
        summary_path,
        audit_summary,
    )

    results[
        "audit_summary"
    ] = audit_summary

    print()
    print(
        "Writing audit manifest"
    )

    _notify_progress(
        progress_callback,
        "manifest",
        "Writing reproducibility manifest",
    )

    manifest_path = (
        _write_audit_manifest(
            config
        )
    )

    results[
        "audit_manifest"
    ] = manifest_path

    print()
    print(
        "=" * 60
    )
    print(
        "TRAJECTORY AUDIT COMPLETED"
    )
    print(
        "=" * 60
    )

    print(
        "Output directory:"
    )

    print(
        config.output_dir
    )

    print()

    print(
        "Manifest:"
    )

    print(
        manifest_path
    )

    print()

    print(
        "Summary:"
    )

    print(
        summary_path
    )

    _notify_progress(
        progress_callback,
        "complete",
        "Trajectory audit complete",
    )

    return results


def _parse_args() -> argparse.Namespace:
    """
    Parse standalone Trajectory Audit CLI arguments.
    """

    parser = argparse.ArgumentParser(
        description=(
            "Run longitudinal trajectory analysis "
            "from a GameBus campaign export."
        )
    )

    parser.add_argument(
        "--campaign-data",
        help=(
            "Path to campaign_data.zip. "
            "Defaults to config/campaign_data.zip."
        ),
    )

    parser.add_argument(
        "--campaign-desc",
        help=(
            "Path to campaign_desc.xlsx. "
            "Defaults to config/campaign_desc.xlsx."
        ),
    )

    parser.add_argument(
        "--output-dir",
        help=(
            "Trajectory Audit output directory. "
            "Defaults to "
            "data_analysis/trajectory_audit."
        ),
    )

    parser.add_argument(
        "--max-cases",
        type=int,
        help=(
            "Maximum number of compact participant "
            "case exports. Defaults to the configured "
            "value."
        ),
    )

    return parser.parse_args()


def main() -> None:

    args = _parse_args()

    config = (
        TrajectoryAuditConfig()
    )

    if args.campaign_data:

        config.campaign_data_path = (
            os.path.abspath(
                args.campaign_data
            )
        )

    if args.campaign_desc:

        config.campaign_desc_path = (
            os.path.abspath(
                args.campaign_desc
            )
        )

    if args.output_dir:

        config.output_dir = (
            os.path.abspath(
                args.output_dir
            )
        )

    if (
        args.max_cases
        is not None
    ):

        if args.max_cases < 0:

            raise ValueError(
                "--max-cases must be "
                "zero or greater."
            )

        config.case_export_max_cases = (
            args.max_cases
        )

    run_trajectory_audit(
        config
    )


if __name__ == "__main__":
    main()